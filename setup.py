from setuptools import setup

setup(name='dis_reconnect',
      version='1.2',
      description='Gaussian and Binomial distributions',
      packages=['dsnd_probability'],
      author = 'Echo Yu',
      author_email = 'echoyu.business@gmail.com',
      zip_safe=False)