from setuptools import setup

setup(name='distributions',
      version='1.2',
      description='Gaussian and Binomial distributions',
      packages=['dsnd_distributions'],
      author = 'Echo Yu',
      author_email = 'echoyu.business@gmail.com',
      zip_safe=False)
